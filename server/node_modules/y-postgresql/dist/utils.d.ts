import * as Y from 'yjs';
import { PgAdapter } from './pg-adapter';
export declare const readStateVector: (db: PgAdapter, docName: string) => Promise<{
    sv: Uint8Array;
    clock: number;
} | {
    sv: null;
    clock: number;
}>;
export declare const getCurrentUpdateClock: (db: PgAdapter, docName: string) => Promise<any>;
/**
 * Store an update in PostgreSQL.
 * @param db
 * @param docName
 * @param update
 * @returns id of the stored document
 */
export declare const storeUpdate: (db: PgAdapter, docName: string, update: Uint8Array) => Promise<any>;
/**
 * Merge all PostgreSQL records of the same yjs document together.
 */
export declare const flushDocument: (db: PgAdapter, docName: string, stateAsUpdate: Uint8Array, stateVector: Uint8Array) => Promise<any>;
export declare const getYDocFromDb: (db: PgAdapter, docName: string, flushSize: number) => Promise<Y.Doc>;
//# sourceMappingURL=utils.d.ts.map