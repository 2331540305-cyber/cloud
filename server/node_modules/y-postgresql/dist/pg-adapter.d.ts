import { PoolConfig, Pool as IPool } from 'pg';
type Update = {
    id: number;
    docname: string;
    value: Uint8Array;
    version: 'v1';
};
export declare class PgAdapter {
    private tableName;
    private pool;
    constructor(tableName: string, pool: IPool);
    /**
     * Create a PostgresqlAdapter instance
     * @param connectionOptions
     * @param param1
     * @param param1.tableName Name of the table where all documents are stored
     * @param param1.useIndex Whether to use an index for the table
     * @returns
     */
    static connect(connectionOptions: PoolConfig, { tableName, useIndex }: {
        tableName: string;
        useIndex: boolean;
    }): Promise<PgAdapter>;
    /**
     * Find the latest document id in the table. Returns -1 if no document is found.
     * @param docName
     * @returns
     */
    findLatestDocumentId(docName: string): Promise<any>;
    /**
     * Store one update in PostgreSQL.
     * @returns {Promise<object>} The stored document
     */
    insertUpdate(docName: string, value: Uint8Array): Promise<any>;
    private _getStateVector;
    /**
     * Get the state vector of a document in PostgreSQL.
     * @param docName
     * @returns
     */
    getStateVectorBuffer(docName: string): Promise<Uint8Array | null>;
    /**
     * Upsert the statevector for one document in PostgreSQL.
     * @param docName
     * @param value
     */
    putStateVector(docName: string, value: Uint8Array): Promise<any>;
    /**
     * Delete all updates of one document in a specific range.
     * @param docName
     * @param from Including this id
     * @param to Excluding this id
     */
    clearUpdatesRange(docName: string, from: number, to: number): Promise<void>;
    /**
     * Get all document updates for a specific document as a cursor.
     */
    readUpdatesAsCursor(docName: string, callback: (records: Update[]) => void): Promise<number>;
    /**
     * Delete a document, and all associated data from the database.
     */
    deleteDocument(docName: string): Promise<any>;
    /**
     * Close the connection to the database.
     */
    close(): Promise<void>;
}
export {};
//# sourceMappingURL=pg-adapter.d.ts.map